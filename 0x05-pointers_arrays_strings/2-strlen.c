#include "main.h"
#include <string.h>

/**
 * _strlen - check the code
 * @s: a variable
 *
 * return: length of the string
 */

int _strlen(char *s)
{
	int i;
	for (i = 0; s[i] != '\0'; i++)
	{
		continue;
	}
	return(i);
}
